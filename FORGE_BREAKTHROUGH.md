# FORGE v5.0 — SINGULARITY
## The breakthrough: one ontology for money and decisions

Most "AI agent" stacks add more workers. FORGE does the opposite.

It collapses **market signal → economic hypothesis → offer → cash truth → capital constraint → institutional learning** into a single closed causal system.

That is the unprecedented property.

---

## What makes it unusual

| Layer | Ordinary systems | FORGE Singularity |
|-------|------------------|-------------------|
| Money | Dashboard widget | `StripeTruth` is the only cash prior |
| Offers | Marketing copy | First-class objects with price + payment link + EV |
| Channels | Ad platform silos | Ranked (offer × channel) bets with updating priors |
| Agents | Chat roles | Contracts inside one state machine |
| Learning | Slack notes | Causal graph + experiment ledger written every cycle |
| Action | "Insights" | Imperatives ordered by capital constraint |

Zero deployable cash ⇒ only $0-capital actions may AUTO-execute.  
Negative balance is not ignored — it is P0.

---

## Operating law

```
observe → rank → act → record → learn → repeat
```

1. **TRUTH** — livemode Stripe balance / charges / subs only  
2. **CAUSAL** — every outreach, ad, sale, no-sale updates (offer × channel) priors  
3. **CASH** — deployable = max(0, available + pending)  
4. **OFFERS** — live Stripe Payment Links are world-model entities  
5. **LOOP** — Perception → Decision → Revenue rank → Action → Outcome → Knowledge  
6. **ORDER** — P0 settlement path · P1 top EV bet · P2 record within 24h · P3 always write state  

---

## Live system (as of last cycle)

```
Available   -$671.58
MRR         $0
Deployable  $0
```

**Top EV bets (zero-capital):**
1. referral × ops_99  
2. referral × audit_49  
3. warm_dm × audit_49  

**Live offers:**
- Setup Audit $49 → https://buy.stripe.com/9B6cMZdXf8Q8a8t8A5aEE00  
- Ops Sub $99/mo → https://buy.stripe.com/cNibIV06p3vO6Wh5nTaEE01  

---

## Run it

```bash
python forge_singularity.py              # full cycle + tower
python forge_singularity.py --protocol   # print the law
python forge_singularity.py --tower      # tower only
python forge_singularity.py --record paid 49 audit_49 warm_dm
```

---

## Stack

| Module | Role |
|--------|------|
| `forge_cognitive_core.py` | State → Perception → Prediction → Decision → Action → Outcome → Learning |
| `forge_revenue_engine.py` | StripeTruth, offers, channel EV ranking, outcome recording |
| `forge_singularity.py` | Closed loop binding cognitive + revenue into one protocol |
| `forge_garcar_bridge.py` | Legacy Garcar sensors → Forge signals |
| `forge_all.py` | Unified launcher |

Protocol ID: **FORGE-SINGULARITY-v5**  
Version: **5.0.0**

---

## The claim

Not more agents.  
Not more repos.  
Not more automation theater.

**One coherent representation of information, software production, financial truth, capital allocation, and organizational learning.**

That is the breakthrough.
